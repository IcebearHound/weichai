# Mock Adapters

Demonstration-only implementations of workflow ports and the static module
tree. Keeping these implementations in a dedicated package makes it explicit
that they do not perform real retrieval, translation, or workspace mutation.
