# Upstream Record

This fixture is a clean-room, behavior-oriented Python implementation of the
core public responsibilities in Apache Commons FileUpload 1.5. It is not an
upstream distribution and does not reuse Java source files. It exists as a
Python retrieval and translation reference for the local Java skeleton.
