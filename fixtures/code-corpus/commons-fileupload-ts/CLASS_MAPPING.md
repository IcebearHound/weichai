# Class Mapping

| Java 1.5 type | TypeScript counterpart |
| --- | --- |
| `FileUploadBase`, `FileUpload` | same names in `src/file-upload.ts` |
| `MultipartStream` | same name and part/boundary operations |
| `ParameterParser` | same name and quoted header parameter behavior |
| `FileItem`, `FileItemFactory`, `RequestContext` | same interfaces in `file-upload.ts` |
| `disk.DiskFileItem`, `disk.DiskFileItemFactory` | same classes in `file-upload.ts` |
| `FileUploadException` and size exceptions | same classes in `file-upload.ts` |
| `util.FileItemHeadersImpl` | `FileItemHeaders` in `file-upload.ts` |
