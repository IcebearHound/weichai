# Class Mapping

| Java 1.5 type | Python counterpart |
| --- | --- |
| `FileUploadBase`, `FileUpload` | same names in `commons_fileupload.core` |
| `MultipartStream` | same name and part/boundary operations |
| `ParameterParser` | same name and quoted header parameter behavior |
| `FileItem`, `FileItemFactory`, `RequestContext` | protocols and concrete implementations in `core` |
| `disk.DiskFileItem`, `disk.DiskFileItemFactory` | same names in `core` |
| `FileUploadException` and size exceptions | same names in `core` |
| `util.FileItemHeadersImpl` | `FileItemHeaders` in `core` |
