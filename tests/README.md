# Repository Tests

Repository-level tests will be grouped into these categories:

- `contract`: shared port and protocol compatibility.
- `integration`: workflow-to-adapter and service boundaries.
- `e2e`: complete user workflows through the application.

Package-local unit and component tests remain beside their source code.
