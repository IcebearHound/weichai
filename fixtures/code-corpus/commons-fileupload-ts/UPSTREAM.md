# Upstream Record

This fixture is a clean-room, behavior-oriented TypeScript implementation of
the core public responsibilities in Apache Commons FileUpload 1.5. It is not
an upstream distribution and does not reuse Java source files. It serves as a
TypeScript retrieval and translation reference for the local Java skeleton.
